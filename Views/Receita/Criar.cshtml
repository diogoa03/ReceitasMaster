@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/mySheet.css" />
    <title>Criar Receita - ReceitasMaster</title>
</head>
<body>
    <h1>🍳 Criar Nova Receita</h1>

    <div class="card">
        <form method="post" action="/Receita/Criar">
            <table class="form-table">
                <tr>
                    <td><label for="titulo">Título:</label></td>
                    <td><input type="text" id="titulo" name="Titulo" required /></td>
                </tr>
                <tr>
                    <td><label for="descricao">Descrição:</label></td>
                    <td><textarea id="descricao" name="Descricao" rows="3" required></textarea></td>
                </tr>
                <tr>
                    <td><label for="instrucoes">Instruções:</label></td>
                    <td><textarea id="instrucoes" name="Instrucoes" rows="6" required placeholder="Descreva o modo de preparo da receita..."></textarea></td>
                </tr>
                <tr>
                    <td><label for="ativa">Ativa:</label></td>
                    <td>
                        <select id="ativa" name="Ativa">
                            <option value="true" selected>Sim</option>
                            <option value="false">Não</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td colspan="2" class="form-buttons">
                        <button type="submit" class="btn btn-primary">✅ Criar Receita</button>
                        <a href="/Receita/Index" class="btn btn-secondary">❌ Cancelar</a>
                    </td>
                </tr>
            </table>
        </form>
    </div>
</body>
</html>